# Hypothesis seeds

Private, user-supplied examples can be stored in `user-seed-cases.json`. That file is intentionally ignored because it may contain private-chat history, entry timing, and unpublished valuation work.

Seed examples are used only to define strategy arms and fixed evaluation horizons. They are never counted as validation evidence without the complete contemporaneous eligible universe, including rejected and losing names.
